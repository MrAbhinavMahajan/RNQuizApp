import { StyleSheet } from "react-native";
import { vpx } from "../../../libraries/responsive-pixels";

export const styles = StyleSheet.create({
  optionsContainer: {
    paddingBottom: vpx(40),
  },
  scrollableOptionsContainer: {
    gap: vpx(18),
  },
});
